// Example of Conditional Statement
// Guess The Age
/*
#include <stdio.h>

int main(void) {

	double operand1, operand2;
	double result;
	char option;

	//printf("Enter input as \"operand operator operand e.g 1 + 2\"\n");
	//scanf_s("%d%c%d", &operand1, &option, &operand2);

	printf("Enter first number");
	// take input in operand1
	printf("Enter second number");
	// take input in operand2
	printf(" Press + for Addition\n - for subtraction\n * for multiplication \n / for division");
	// take input in option

	operand1 = 3, operand2 = 4;
	option = '+';
	switch (option) {

	case '+':
		result = operand1 + operand2;
		printf("%lf", result);
		break;
	case '-':
		result = operand1 - operand2;
		printf("%lf", result);
		break;
	case '*':
		result = operand1 * operand2;
		printf("%lf", result);
		break;
	case '/':
		result = operand1 / operand2;
		printf("%lf", result);
		break;
	}
	printf("Bye");
	return 0;
}*/