#include <stdio.h>
#include <stdlib.h>
#define COUNT 5

//int main() {
	//int index, elements[COUNT], sum = 0;  // array for 5 integers 
	//int copyArray[COUNT];

	 // Read in 5 elements. 
	//printf("Enter %d integers:",COUNT);
	//copyArray[0] = elements[0];

	/*
	// To input values in arrays using scanf or rand()
	for (index = 0; index < COUNT; index++) {
		printf("Enter %d integers:", index);
		scanf_s("%d", &elements[index]);

		//elements[index] = rand()%100;
		printf("%d\n", elements[index]);
	}*/
	
	/* 
	// adding values in array
	printf("The integers are");
	for (index = 0; index < COUNT; index++) {
		//sum = sum + elements[index];
		printf("%d\n", elements[index]);
		//printf("\n");
	}*/
/*
	// Adding 2 adjacent values and assigning it back to current index
	for (index = 1; index < COUNT; index++) {
		elements[index] = elements[index - 1] + elements[index];
		printf("%d", elements[index]);
		printf("\n");
	}

	elements[0] = elements[0] + elements[index];
	//printf("Sum =%d", sum);
	return 0;

}
*/